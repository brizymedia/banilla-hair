# CLAUDE.md — 바닐라 헤어 홈페이지

이 저장소로 작업할 때 Claude가 참고할 프로젝트 지침입니다.

## 프로젝트 개요

전남 광양시 광양읍 미용실 **바닐라 헤어(Banilla Hair)** 의 고객 홍보용 홈페이지.
빌드 도구·프레임워크 없는 **순수 정적 사이트**이며, GitHub Pages로 배포됩니다.

- 라이브: https://brizymedia.github.io/banilla-hair/
- 배포: `main` 브랜치에 푸시하면 GitHub Pages가 자동 재빌드 (1~2분)

## 매장 정보 (수정 시 아래 값 유지)

| 항목 | 값 |
|---|---|
| 상호 | 바닐라 헤어 (Banilla Hair) |
| 주소 | 전라남도 광양시 광양읍 서평8길 23 |
| 예약 전화 | 010-7137-6560 |
| 영업시간 | 10:00 – 19:00 |
| 휴무 | **매주 화요일** (일요일은 정상 영업) |

전화번호는 `tel:` 링크와 표시 문구 **양쪽 모두**에 들어 있으니 변경 시 전부 바꿔야 합니다.

## 파일 구조

```
index.html      메인 페이지 (히어로 · 소개 · 시술메뉴 · 갤러리 · 스타일찾기 · 영업안내 · 오시는길)
갤러리.html      관리자용 갤러리 관리 (사진 압축 업로드 → GitHub 저장 → 공개)
추천.html        고객용 얼굴형 분석 맞춤 헤어스타일 추천
gallery/photos.json   업로드된 갤러리 사진 목록(매니페스트)
logo.png        배경 투명 처리된 공식 로고
```

이미지 명명 규칙 (한글 파일명 사용 중, GitHub Pages에서 정상 동작 확인됨):
- `갤러리1~6, 11~16.jpg` — 메인 스타일 갤러리 (7~10번은 존재하지 않음)
- `남자스타일1~3.jpg`, `여자스타일1~6.png` — 얼굴형별 추천 스타일 차트
- `원장님1~3.jpg` — 히어로 슬라이드쇼
- `바닐라헤어.jpg` — 히어로 대표 이미지

## 디자인 규칙

- 컬러: 간판의 핫핑크 `--pink:#e0006c` / `--pink-deep:#b60057`, 크림 배경 `--cream:#fbf6f2`
- 폰트: 제목 `Cormorant Garamond`(이탤릭 강조), 본문 `Noto Serif KR`, 보조 `Gowun Dodum`
- **본문 글씨는 `font-weight:500` 유지** — 사장님이 시인성 때문에 두껍게 요청함. 300으로 되돌리지 말 것
- 한글 줄바꿈 깨짐 방지: 긴 문구에 `word-break:keep-all`
- CSS/JS는 각 HTML 파일에 인라인으로 작성 (외부 빌드 없음)

## 얼굴형 추천 로직 (추천.html)

얼굴 랜드마크로 비율을 측정해 6종으로 분류하고, 성별에 따라 다른 차트·문구를 보여줍니다.

- `L = 얼굴길이/광대폭`, `J = 턱폭/광대폭`, `F = 이마폭/광대폭`
- 분류: 세로로 긴(L≥1.55) · 가로폭 긴(L≤1.18) · 각진(J≥0.82) · 둥근(L<1.32, J≥0.74) · 가로폭 짧은 · 계란형
- 얼굴 인식 실패 시 **직접 선택 화면으로 폴백** — 이 폴백은 반드시 유지할 것
- 사진은 기기 안에서만 처리하며 서버로 전송하지 않음. 이 문구와 실제 동작이 어긋나지 않게 할 것

## 배포

```bash
git add -A && git commit -m "변경 내용" && git push origin main
```

푸시 후 GitHub Pages 빌드 상태 확인:

```bash
gh api repos/brizymedia/banilla-hair/pages --jq .status
```

## 주의사항

- 이미지 파일을 추가하면 **참조하는 HTML도 함께 수정**해야 갤러리에 표시됩니다
- `node_modules/`와 로고 원본 백업은 `.gitignore` 처리됨 (사이트에서 참조하지 않음)
- 사장님은 개발자가 아니므로, 작업 결과는 **전문용어 없이 한국어로** 설명할 것
