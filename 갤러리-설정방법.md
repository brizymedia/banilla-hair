# 갤러리 업로드 설정 방법

한 번만 해두면 그다음부터는 **비밀번호만 넣고** 사진을 올릴 수 있습니다.
휴대폰이든 PC든, 기기를 바꿔도 주소와 비밀번호만 넣으면 바로 됩니다.

전체 20분 정도 걸립니다. 순서대로 따라 하시면 됩니다.

---

## 왜 이렇게 하나요

홈페이지는 서버가 없는 방식이라, 사진을 저장하려면 누군가 대신 저장해 줄 곳이 필요합니다.
구글 앱스스크립트가 그 역할을 합니다.

```
사장님 휴대폰  →  구글 앱스스크립트  →  GitHub 저장소  →  홈페이지 갤러리
   (비밀번호)       (토큰 보관)
```

비밀번호만 사장님이 들고 계시고, **GitHub 토큰은 구글 서버 안에만** 있습니다.
휴대폰을 잃어버려도 토큰은 새어나가지 않습니다.

---

## 1단계 · GitHub 토큰 만들기 (5분)

1. https://github.com/settings/personal-access-tokens/new 접속 (GitHub 로그인)
2. **Token name**: `바닐라헤어 갤러리`
3. **Expiration**: `1 year` 권장
4. **Repository access**: `Only select repositories` 선택 → **banilla-hair** 만 고르기
5. **Permissions** → **Repository permissions** → **Contents** 를 **Read and write** 로 변경
6. 맨 아래 **Generate token** → 나온 토큰을 **복사** (한 번만 보여줍니다)

> 이 토큰은 **banilla-hair 저장소 하나만** 건드릴 수 있습니다.

---

## 2단계 · 앱스스크립트 만들기 (10분)

1. https://script.google.com 접속 (구글 로그인)
2. **새 프로젝트** 클릭
3. 왼쪽 `코드.gs` 안의 내용을 **모두 지우고**,
   이 폴더의 **`갤러리-AppsScript-코드.gs`** 내용을 **전부 복사해서 붙여넣기**
4. 프로젝트 이름을 `바닐라헤어 갤러리` 로 변경 (왼쪽 위)
5. 💾 저장

### 설정값 넣기

6. 왼쪽 톱니바퀴 **프로젝트 설정** 클릭
7. 맨 아래 **스크립트 속성** → **스크립트 속성 추가** 로 아래 5개를 넣습니다

| 속성 이름 | 값 |
|---|---|
| `UPLOAD_PW` | 사장님이 정한 업로드 비밀번호 |
| `GH_TOKEN` | 1단계에서 복사한 토큰 |
| `GH_OWNER` | `brizymedia` |
| `GH_REPO` | `banilla-hair` |
| `GH_BRANCH` | `main` |

8. **스크립트 속성 저장**

> 비밀번호는 사장님이 기억하기 쉬운 것으로 정하시되, 남이 못 맞출 만한 것으로 해주세요.

---

## 3단계 · 배포하기 (5분)

1. 오른쪽 위 **배포** → **새 배포**
2. 톱니바퀴 ⚙️ → **웹 앱** 선택
3. 설정을 이렇게 맞춥니다

| 항목 | 값 |
|---|---|
| 설명 | 갤러리 업로드 |
| 다음 사용자로 실행 | **나** |
| 액세스 권한이 있는 사용자 | **모든 사용자** |

4. **배포** 클릭
5. **권한 승인** 창이 뜨면 승인합니다
   - "이 앱은 확인되지 않았습니다" 경고가 나오면
     **고급** → **바닐라헤어 갤러리(안전하지 않음)으로 이동** 클릭
   - 사장님이 직접 만든 스크립트라 안전합니다
6. 나온 **웹 앱 URL** 을 복사 (`https://script.google.com/macros/s/.../exec` 형태)

> **액세스 권한을 「모든 사용자」로** 하지 않으면 업로드가 안 됩니다.
> 대신 비밀번호가 있어서 아무나 올리지는 못합니다.

---

## 4단계 · 홈페이지에 연결 (1분)

1. https://brizymedia.github.io/banilla-hair/갤러리.html 접속
2. **최초 설정** 칸에 3단계에서 복사한 **웹 앱 URL** 붙여넣기 → **주소 저장**
3. 오른쪽 위 표시가 **연결됨** 으로 바뀌면 완료입니다

---

## 이제부터 사진 올리는 법

1. 갤러리 관리 페이지 열기
2. **사진 고르기** (여러 장 한 번에 가능, 휴대폰은 카메라도 열림)
3. 설명 적기 (선택)
4. **비밀번호** 넣기 (기억 체크해두면 다음부터 자동)
5. **갤러리에 올리기**

1~2분 뒤 홈페이지 갤러리에 나타납니다. 바로 안 보인다고 다시 올리지 마세요.

---

## 문제가 생기면

| 증상 | 해결 |
|---|---|
| "연결 실패" 표시 | 웹 앱 URL이 `/exec` 로 끝나는지 확인 |
| "서버가 이상한 답을 보냈습니다" | 배포 설정에서 **액세스 권한 = 모든 사용자** 확인 |
| "비밀번호가 다릅니다" | 스크립트 속성의 `UPLOAD_PW` 와 같은지 확인 |
| "서버에 GitHub 토큰이 설정되지 않았습니다" | 스크립트 속성에 `GH_TOKEN` 이 있는지 확인 |
| "GitHub 오류 401" | 토큰이 만료됨. 1단계를 다시 하고 `GH_TOKEN` 교체 |
| "GitHub 오류 403" | 토큰 권한 확인 (Contents = Read and write) |
| 코드를 고친 뒤 반영이 안 됨 | **배포 → 배포 관리 → 편집(연필) → 버전을 새 버전으로 → 배포** |

---

## 토큰을 새로 바꿀 때

토큰은 1년 뒤 만료됩니다. 그때는 **1단계만 다시** 하고,
앱스스크립트 **프로젝트 설정 → 스크립트 속성**에서 `GH_TOKEN` 값만 바꾸면 됩니다.
홈페이지 쪽은 손댈 것이 없습니다.
